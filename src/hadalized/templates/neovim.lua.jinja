-- WARNING: Auto-generated by {{ theme.app_info }}

vim.cmd([[ highlight clear ]])


local theme_meta = {{ theme.meta.model_dump_lua() }}
local palette = {{ palette.model_dump_lua() }}

vim.o.bg = "{{ palette.mode }}"

-- Highlight groups are set from a mapping
-- neovim hl group -> theme field -> palette color
{% for group, style in utils.neovim.mapping(theme).items() -%}
{% if style -%}
vim.api.nvim_set_hl(0, "{{ group }}", {{ utils.neovim.style_dump_lua(style) }})
{% else -%}
-- Excluding: {{ group }}
{% endif -%}
{% endfor -%}
